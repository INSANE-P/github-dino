# GitHub Personal Access Token (classic)
# 발급: github.com → Settings → Developer settings → Personal access tokens (classic)
# 스코프: read:user, public_repo 면 충분
# 이 파일을 .env.local로 복사한 뒤 토큰을 채우세요 (.env.local은 gitignore 됨)
VITE_GITHUB_TOKEN=여기에_토큰
